<window type="dialog" id="114">
	<defaultcontrol>702</defaultcontrol>
	<visible>Player.HasMedia + Window.IsActive(PlayerControls) + !Window.IsActive(FullscreenVideo) + !Window.IsActive(Visualisation)</visible>
	<controls>
		<control type="group">
			<include>open_PlayerControls</include>
			<include condition="Player.HasAudio">globalMusicControls</include>
			<include condition="Player.HasVideo">globalVideoControls</include>
		</control>
	</controls>
</window>
